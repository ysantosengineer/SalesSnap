# Testing standards

Each stage adds tests proportional to the functionality introduced. Backend testing uses pytest and prioritizes services, business rules, relevant endpoints, data transformations, and analytical algorithms. Frontend tests should cover components or flows only where they add real value.

Do not chase 100% coverage as a metric; tests must protect meaningful behavior.
